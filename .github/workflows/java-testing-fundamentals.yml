name: Java testing fundamentals test

on:
  push:
    paths:
      - 'courses/java-testing-fundamentals/**'
      - '.github/workflows/java-testing-fundamentals.yml'
  pull_request:
    paths:
      - 'courses/java-testing-fundamentals/**'
      - '.github/workflows/java-testing-fundamentals.yml'

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: java_quiz_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20

    defaults:
      run:
        working-directory: courses/java-testing-fundamentals

    env:
      QUIZ_TEST_DB_URL: jdbc:mysql://127.0.0.1:3306/java_quiz_test
      QUIZ_TEST_DB_USERNAME: root
      QUIZ_TEST_DB_PASSWORD: root

    steps:
      - name: 저장소 받기
        uses: actions/checkout@v4

      - name: Java 17 준비
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle 캐시 준비
        uses: gradle/actions/setup-gradle@v4

      - name: 테스트 실행
        run: ./gradlew test --no-daemon

      - name: 검사 리포트 남기기
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: java-testing-fundamentals-report
          path: courses/java-testing-fundamentals/build/reports/tests/test
